# Final setup
Upload everything in this ZIP to the root of `laxman824/laxman824`, preserving `.github/workflows/github-snake.yml`.
Enable GitHub Actions and run `Generate Contribution Snake` once.

The two source images are also included under `assets/`.
The SVGs embed the images directly, so the animated banner and ID card are self-contained.

Note: the email is preserved exactly as supplied: `laxmankethavath5@gmaill.com`. Correct it in README.md if `gmaill.com` was a typo.


ID card updated to use the real café photo with only circular cropping and subtle border effects.
